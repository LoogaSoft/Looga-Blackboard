# Looga Blackboard

Typed blackboard keys, definitions, runtime values, and editor tooling for designer-authored game state.

Use this package when multiple systems need to publish or read shared runtime state without string keys.
